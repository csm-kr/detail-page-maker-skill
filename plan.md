# Detail Page Maker Skill 계획

## 1. 목적

기존 `coupang-detail-page-maker`를 기준 스킬로 가져와, Behance의 우수한 제품 상세페이지 수준으로 완성도가 높고 요소별 수정이 가능한 상용 HTML 상세페이지 제작 스킬을 만든다.

최종 결과는 통이미지 상세페이지가 아니다. 텍스트, 이미지, GIF, 레이아웃, 색상, 타이포그래피를 각각 교체하거나 수정할 수 있는 HTML/CSS 구조여야 한다.

## 2. 핵심 차별점

- 기존 스킬의 상품 조사, 근거 관리, 기획, 카피, 제품·모델 동일성, QA 자산을 재사용한다.
- 최종 산출물의 중심을 `800×2400 PNG 여러 장`에서 `수정 가능한 HTML 상세페이지`로 전환한다.
- 생성형 시각 자산에는 OpenAI `imagegen`만 사용한다.
- 제품 작동·효과·사용 전후와 같이 시간 변화가 필요한 장면은 HyperFrames로 제작한다.
- HyperFrames 결과를 짧은 GIF로 만들어 관련 주장이나 설명 바로 옆에 삽입한다.
- Behance 작품을 그대로 복제하지 않고, 제품군별 우수 사례에서 구성·밀도·타이포·아트디렉션 문법을 추출한다.

## 3. 입력

### 최초 입력

- 숫자 상품번호가 포함된 도매꾹 실제 상품 상세 URL 1개

최초 실행에서는 다른 자료를 요구하지 않는다. 기존 `dmk-extractor`로 도매꾹 URL을 열어 상품명, 대표·상세 이미지, 상세 내부 GIF, 최근 6개월 공개 후기와 검증 원장을 portable bundle로 저장한다. 새 스킬은 검증된 번들 위에서 구성, 옵션, 규격, 소재, 사용법, 주의사항과 공급처가 명시한 제품 사실을 `supplier-facts.json`으로 정규화한다.

도매꾹 원문은 한 덩어리로 요약하지 않고 다음 네 층으로 분리한다.

1. `supplier-photo-inventory.json`: 대표·상세 원본 사진을 제품 동일성, 디테일, 치수, 사용 시연, 제외 자산으로 분류한다.
2. `supplier-facts.json`: 치수·재질·구조·구성·사용 정보와 변동 운영 정보를 원본 locator와 함께 저장한다.
3. `supplier-appeal-candidates.json`: 공급처 소구점을 사실 ID에 연결하고 검증 전 과장 확장을 막는다.
4. `supplier-planning-brief.json`: 제품 질문, 구매 서사, 필요한 이미지·모션과 금지 주장을 정리한다.

소구 후보와 기획은 공급처 사실에서 파생되지만 그 자체가 제품 사실의 출처는 아니다. 게시 가능한 값은 원본에서 확인되고 사람의 승인을 받은 사실로 제한한다.

### 공급처 추출 후 선택 입력

- 사용자가 직접 촬영한 동일 SKU의 다각도 제품 사진
- 경쟁 상품 상세페이지와 구매후기
- 브랜드 로고, 컬러, 폰트, 디자인 가이드
- 사용자가 지정한 Behance 참고 작품
- 실측·검사·시연 결과와 기타 주장 근거

선택 입력은 공급처 원문 추출이 끝난 뒤 콘텐츠 전략과 근거 보강에만 사용한다. 공급처의 SKU 사실을 다른 판매자 페이지나 생성 결과로 덮어쓰지 않는다.

사용자 촬영 사진은 제품 이해와 동일성 강화를 위한 사실 SSOT에 포함한다. ImageGen이 생성한 결과는 원본 사진을 대체하는 사실 근거가 아니라, 승인된 파생 제품 뷰 시트로 관리한다.

## 4. 전체 제작 흐름

### 4.1 근거 수집과 제품 사실 잠금

1. 숫자 상품번호가 포함된 도매꾹 실제 상품 상세 URL 1개를 최초 입력으로 받는다.
2. 기존 `dmk-extractor`로 격리된 portable bundle을 만든다.
3. 대표 이미지, 펼친 상세 원본, 상세 내부 GIF 전체 프레임, 최근 6개월 공개 후기와 녹화 증거를 보존한다.
4. 수집 시각, 요청·최종 URL, 원본 경로, 파일 크기·치수와 SHA-256을 포함한 변경 불가능한 공급처 스냅샷을 만든다.
5. 검증된 bundle에서 역할이 붙은 원본 사진 인벤토리와 출처 locator가 붙은 `supplier-facts.json`을 만든다.
6. 확인된 치수·제품 사실, 공급처 소구 문구, 검증 전 소구 후보, 기획 가설과 변동 운영 정보를 구분한다.
7. 실제 제품 이미지의 해시와 파일 목록을 제품 동일성의 기준으로 잠근다.
8. 공급처 페이지에 없는 사실은 추정하거나 ImageGen 결과에서 역으로 만들어내지 않는다.
9. 근거가 없는 성능·효과·인증 주장은 상세페이지에 사용하지 않는다.
10. 페이지가 로그인·권한·캡차로 막히거나 캡처 검증이 실패하면 정상 결과로 승격하지 않는다.
11. 필수 사실이 누락되면 캡처는 보존하되 해당 주장·섹션을 차단하고 사용자 확인을 요청한다.

상세 계약은 [`docs/contracts/domeggook-supplier-extraction.md`](docs/contracts/domeggook-supplier-extraction.md)를 따른다.

첫 실제 추출 prototype은 `https://domeggook.com/43314131?from=popular100`으로 수행한다. 이전 `dmk-extractor`가 탐지한 `within_detail_root: true` 상세 자산만 사실 정규화와 제품 SSOT에 자동 승격하고, root 밖 장문 이미지 fallback은 수동 검수 전까지 보류한다.

prototype에서 portable bundle 생성과 독립 무결성 검증은 통과했다. 실제 원본에서 제품 사진, 표시 치수, 재질·구조, 소구 후보와 기획 브리프를 분리할 수 있음을 확인했다. 동시에 다음 보완 사항을 발견했다.

- 상세 자산별 `within_detail_root`, 선택 방식과 root selector가 출력에 남아야 한다.
- GIF MIME 원본 수와 실제 다중 프레임 동적 GIF 수를 분리해야 한다. 첫 fixture의 GIF 원본 6개는 모두 1프레임 정지 GIF였다.
- 가격, 최소 주문수량과 옵션은 수집 시각을 가진 변동 정보로 추가 보존해야 한다.
- 이미지 속 치수와 문구는 OCR 결과, 원본 영역과 사람의 확인 상태를 함께 기록해야 한다.
- 특정 상품번호나 한 판매자 레이아웃에 고정되지 않도록 서로 다른 도매꾹 상품 fixture로 회귀 검증해야 한다.

상세 결과는 [`prototypes/domeggook-43314131/report.md`](prototypes/domeggook-43314131/report.md)와 [`supplier-facts.json`](prototypes/domeggook-43314131/supplier-facts.json)에 남긴다.

### 4.2 Behance 품질 표본 구축

1. Behance의 `상세페이지` 검색 결과에서 제품군별 우수 작품을 선별한다.
2. 조회수만으로 선정하지 않고 다음 항목을 기준으로 평가한다.
   - 첫 화면의 제품·혜택 전달력
   - 섹션 간 시각적 리듬
   - 타이포그래피 위계
   - 제품과 배경의 재질·조명 일관성
   - 기능 설명과 시각적 증거의 연결
   - 긴 페이지에서의 색상·레이아웃 변화
   - 구매 결정을 돕는 정보 구조
3. 작품별 스타일을 복사하지 않고 재사용 가능한 디자인 규칙으로 추출한다.
4. 제품군과 브랜드 포지션에 맞는 한 가지 디자인 방향을 선택하고 페이지 전체에 고정한다.

완료된 제품군 조사는 [`research/behance-detail-page-design-grammar.md`](research/behance-detail-page-design-grammar.md)를 따른다. 공통 기본 서사는 `제품·가치 인지 → 상황·문제 → 해결 원리·장점 → 구조·수치 증거 → 사용 → 구매 확인`이며, 감성·정보·증거 블록을 교차하고 최소 세 가지 카메라 거리를 사용한다. HTML 제작 때마다 읽는 압축 규약은 루트의 [`commetial-detail-page.md`](commetial-detail-page.md)로 관리한다.

### 4.3 디자인 디렉션 잠금

HTML 제작 전 다음 항목을 승인 가능한 디자인 명세로 만든다.

- 디자인 콘셉트와 핵심 감정
- 제품군에 맞는 재질·공간·조명
- 기본·보조·강조 색상
- 제목·본문·수치·캡션 타이포 체계
- 섹션 폭, 그리드, 여백, 곡률, 그림자 규칙
- 제품 사진의 시점, 크기, 방향, 광원
- 정보형·감성형·증거형 섹션의 교차 리듬
- GIF가 필요한 주장과 정지 이미지로 충분한 주장

디자인 명세가 승인되기 전에는 최종 이미지, GIF, HTML을 제작하지 않는다.

AI 디자인 계약의 근거와 상세 QA 루프는 [`research/ai-design-skills.md`](research/ai-design-skills.md)를 따른다. 생성 과정은 `디자인 디렉션 잠금 → 토큰·콘텐츠·DOM 구현 → 정적 감사 → 다중 폭 시각 회귀 → 접근성·성능 → 사람의 아트디렉션 비평 → 국소 수정` 순서로 고정한다.

외부 디자인 지식은 [`study-desing-skill.md`](study-desing-skill.md)에 출처·시험한 규칙·채택 여부·실패 이유를 누적한다. `awesome-design-md`와 `ai-design-skills` 원문을 시작점으로 삼되, 새 규칙은 실제 상세페이지에 적용하고 브라우저 QA에서 개선이 확인된 경우에만 영구 규약으로 승격한다.

### 4.4 수정 가능한 HTML 설계

최종 HTML은 다음 원칙을 따른다.

- 카피를 이미지에 굽지 않고 실제 HTML 텍스트로 표현한다.
- 디자인 토큰을 CSS 변수로 분리한다.
- 섹션을 독립 컴포넌트 또는 명확한 DOM 블록으로 나눈다.
- 상품 데이터와 카피는 가능하면 별도 JSON 또는 구조화 데이터로 분리한다.
- 이미지와 GIF는 `assets/`에서 교체할 수 있게 한다.
- 배경, 장식, 강조 효과는 CSS/SVG로 구현하되 제품 자체를 왜곡하지 않는다.
- 데스크톱 편집 화면과 실제 판매 채널 폭에서 모두 검수할 수 있게 한다.
- 폭·패널 수·패널 비율은 전역 고정값이 아니라 판매 채널 preset과 디자인 서사가 결정한다.
- 접근성용 대체 텍스트와 모션 감소 설정을 제공한다.

### 4.5 실제 상품 이미지 생성

실제 상품이 필요한 모든 생성 장면은 제품 참조 이미지를 `imagegen`에 함께 전달한다.

1. 공급처 이미지와 사용자가 촬영한 다각도 사진을 제품 사실 SSOT로 등록한다.
2. 각 입력 사진을 `정면`, `후면`, `좌측`, `우측`, `상단`, `하단`, `3/4`, `로고`, `조작부`, `연결부`, `구성품`, `크기 비교` 역할로 표시한다.
3. 원본 파일의 해시, 촬영 방향, 보이는 부품과 가려진 영역을 manifest에 기록한다.
4. 각 촬영 원본에서 배경을 제거한 투명 PNG 제품 컷아웃 SSOT를 먼저 만든다.
5. ImageGen에는 각 원본과 승인된 컷아웃을 `reference image`로 명시하고 `product-mockup` 용도로 한 뷰씩 별도 생성한다.
6. 제품의 실루엣, 비율, 색상, 로고, 부품 수, 결합 구조를 변경 금지 조건으로 모든 프롬프트에 반복한다.
7. 제품만 보이는 정면, 후면, 좌우 측면, 상단, 3/4, 핵심 디테일 제품 뷰 시트를 만든다.
8. 각 뷰는 동일한 배경, 카메라 높이, 렌즈 감각, 조명, 크기와 여백 규칙을 사용한다.
9. 생성 결과를 공급처 이미지, 사용자 원본 사진과 제품 컷아웃 SSOT에 다시 비교한다.
10. 원본에서 확인할 수 없는 면이나 구조를 ImageGen이 추정한 경우 승인하지 않는다.
11. 동일성 오류가 있으면 전체 묶음이 아니라 해당 뷰만 한 가지 수정 지시로 다시 생성한다.
12. 사용자가 제품 뷰 시트를 승인한 뒤에만 디자인 장면과 HyperFrames 모션 제작에 사용한다.
13. 디자인 명세에 맞는 배경, 조명, 카메라와 사용 상황은 승인된 제품 컷아웃 SSOT와 제품 뷰 시트를 참조해 별도로 생성한다.

ImageGen은 시각 자산 생성에만 사용하며, 상품 사실이나 광고 근거를 새로 만들어내지 않는다.

### 4.5.1 사용자 촬영 사진 체크리스트

- 같은 SKU와 같은 판매 구성을 촬영한다.
- 정면, 후면, 좌측, 우측, 상단, 하단과 45도 3/4 방향을 포함한다.
- 로고, 조작부, 연결부, 재질, 결합 구조와 구성품을 근접 촬영한다.
- 무필터 원본을 사용하고 반사·흔들림·과노출·손가락 가림을 피한다.
- 크기를 판단할 수 있도록 자 또는 알려진 크기의 기준물을 한 장에 포함한다.
- 회전시켜도 광원과 촬영 거리가 크게 달라지지 않게 한다.
- 원본 파일을 리사이즈하거나 메신저 압축하지 않고 전달한다.

### 4.5.2 제품 컷아웃 SSOT

각 촬영 원본에서 제품만 남긴 투명 배경 PNG를 만들고, 이를 디자인 합성·제품 장면 생성·HyperFrames 모션의 공통 비주얼 기준으로 사용한다.

1. 원본 사진을 변경하지 않고 `user-photos/original/`에 보존한다.
2. 각 원본을 ImageGen 편집의 `edit target`으로, 다른 각도 사진을 제품 동일성 `reference image`로 명시한다.
3. 제품 자체는 바꾸지 않고 배경만 균일한 크로마 키 색상으로 교체한다.
4. 제품 색상과 겹치지 않는 키 색상을 선택한다. 기본은 `#00ff00`, 녹색 제품은 `#ff00ff`를 사용한다.
5. 그림자, 바닥면, 반사, 배경 그라데이션과 광고 문구를 만들지 않는다.
6. ImageGen 결과를 프로젝트로 복사한 뒤 설치된 `remove_chroma_key.py`로 알파 PNG를 만든다.
7. 원본과 누끼 결과를 비교해 실루엣, 로고, 작은 구멍, 얇은 부품, 버튼, 케이블과 반투명 재질이 보존됐는지 검수한다.
8. 투명 모서리, 제품 점유율, 가장자리 프린지, 색 번짐과 잘림을 검사한다.
9. 체크무늬, 밝은색, 어두운색 배경 위에서 각각 합성 검수를 한다.
10. 승인된 컷아웃만 `product-ssot/cutout/`에 등록하고 원본 해시와 파생 관계를 manifest에 기록한다.

유리, 액체, 망사, 털, 연기, 반투명·고반사 소재처럼 단순 크로마 제거가 제품을 훼손할 수 있는 경우 자동 승인하지 않는다. 기본 경로가 실패하면 사용자에게 결과와 이유를 보여주고 별도 투명 처리 방법의 승인을 받는다.

### 4.6 ImageGen 이미지에서 GIF까지

제품 효과나 작동을 보여줘야 하는 장면은 다음 파이프라인을 사용한다.

1. `imagegen`으로 제품 동일성이 유지된 기준 이미지 또는 시작·종료 상태 이미지를 만든다.
2. HyperFrames HTML 컴포지션에서 승인된 제품 컷아웃 한 장을 제품 본체 DOM으로 유지하고 배경, 마스크, SVG, 카피 없는 보조 그래픽을 별도 레이어로 분리한다.
3. DOM의 `data-*` 타이밍과 paused seek timeline으로 결정적 동작을 구성한다.
4. 제품이 실제로 변형되는 것처럼 보이게 하지 않고 위치, 회전, 확대, 마스크, 조명, 입자, 전후 비교 등 검증 가능한 효과만 사용한다.
5. 제작·QA용 30fps 무음 MP4를 보존하고 프레임·제품 동일성·루프 연결을 검수한다.
6. 게시용은 HyperFrames에서 15fps·무음·무한 루프·불투명 GIF로 직접 렌더링한다.
7. 용량 예산을 넘을 때만 PNG sequence와 FFmpeg 두 단계 팔레트 후처리를 사용한다.
8. 관련 HTML 섹션의 주장 바로 옆에 GIF를 삽입한다.

한 GIF는 한 가지 동작 또는 효과만 설명한다. 장식만을 위한 자동 재생 GIF는 만들지 않는다.

첫 구현에서는 커터 두 위치를 순차 강조하는 800×720 GIF와, ImageGen start/end keyframe을 방향성 mask로 연결한 800×600 오이 얇게 썰기 GIF를 각각 4초·15fps로 만들었다. 두 원본 HyperFrames 프로젝트는 `videos/dual-blade-motion/`과 `videos/use-demo-motion/`에 보존한다.

## 5. HyperFrames 적용 원칙

- HyperFrames는 생성형 영상 모델이 아니라 HTML 기반 모션 제작·렌더링 계층으로 사용한다.
- 기존 스킬의 LTX 2.3 영상 생성 의존성은 새 스킬의 기본 흐름에서 제거한다.
- 제품 참조와 ImageGen이 정지 이미지의 동일성을 책임진다.
- HyperFrames가 장면 타이밍, 전환, 마스크, 카메라 효과와 렌더링을 책임진다.
- 최종 HTML에는 15fps·무음·무한 루프·불투명 GIF를 기본으로 사용한다.
- 제작·QA를 위해 원본 HyperFrames 컴포지션과 30fps MP4도 보존한다.
- 현재 HyperFrames GIF 구현은 JPEG 중간 프레임을 사용하므로 투명 GIF에 의존하지 않는다.
- 상세 계약은 [`research/hyperframes-gif-pipeline.md`](research/hyperframes-gif-pipeline.md)를 따른다.
- `prefers-reduced-motion` 환경에서는 GIF 대신 대표 정지 이미지를 제공한다.

## 6. 예상 산출물 구조

```text
detail-page-project/
├─ index.html
├─ styles.css
├─ content.json
├─ assets/
│  ├─ product-ssot/
│  │  ├─ supplier/
│  │  ├─ user-photos/
│  │  │  ├─ original/
│  │  │  └─ chroma-source/
│  │  ├─ cutout/
│  │  ├─ product-view-sheet/
│  │  └─ manifest.json
│  ├─ generated/
│  ├─ motion/
│  │  ├─ hyperframes/
│  │  ├─ video/
│  │  └─ gif/
│  └─ fonts/
├─ planning/
│  ├─ supplier-photo-inventory.json
│  ├─ supplier-facts.json
│  ├─ supplier-appeal-candidates.json
│  ├─ supplier-planning-brief.json
│  ├─ product-facts.json
│  ├─ evidence-map.json
│  ├─ design-direction.md
│  ├─ section-storyboard.md
│  └─ behance-reference-analysis.md
└─ qa/
   ├─ product-identity-report.md
   ├─ html-visual-report.md
   ├─ motion-report.md
   └─ acceptance-report.md
```

구현 과정에서 기존 스킬과 HyperFrames의 실제 계약에 맞춰 파일명은 조정할 수 있다.

## 7. 품질 기준

### 디자인

- 첫 화면만 보고 제품 종류, 핵심 가치, 브랜드 분위기를 이해할 수 있어야 한다.
- 페이지 전체가 동일한 조명, 컬러, 타이포, 제품 비율을 유지해야 한다.
- 섹션이 반복 템플릿처럼 보이지 않고 정보 목적에 따라 구도가 달라져야 한다.
- Behance 표본과 비교했을 때 아마추어 템플릿 수준의 여백·타이포·합성 오류가 없어야 한다.

### 제품 동일성

- 로고, 형태, 부품, 색상, 수량, 결합 구조가 실제 제품과 일치해야 한다.
- 사람이 등장할 때 손가락, 접촉, 잡는 방향과 제품 크기가 자연스러워야 한다.
- ImageGen 결과와 GIF 전 구간에서 동일한 SKU로 보여야 한다.
- 모든 디자인·모션 장면이 승인된 제품 컷아웃 SSOT 또는 그 출처가 명확한 제품 뷰 시트를 참조해야 한다.

### 제품 진실성과 기획

- HTML에 게시된 모든 제품 사실은 원본 locator, 확인 상태와 fact ID를 가져야 한다.
- 치수는 숫자·단위뿐 아니라 치수선이 가리키는 부위와 방향이 일치해야 한다.
- 공급처 소구 문구, 소구 후보와 기획 가설은 확인된 제품 사실과 별도 상태로 관리해야 한다.
- 소구 후보는 하나 이상의 사실 ID에 연결하고 근거보다 강한 성능·효과 표현으로 확대하지 않아야 한다.
- `publishable: true`는 원본을 사람이 확인해 `publication_status: confirmed`가 된 사실에만 허용한다.

### 제품 컷아웃

- 파일은 알파 채널이 있는 PNG이며 네 모서리가 완전히 투명해야 한다.
- 원본 대비 제품 윤곽, 로고, 구멍, 버튼, 케이블과 얇은 부품의 손실이 없어야 한다.
- 키 색상 프린지, 배경 잔여물, 잘린 가장자리와 가짜 그림자가 없어야 한다.
- 모든 각도가 동일한 캔버스, 기준선, 크기와 안전 여백 규칙을 따라야 한다.
- 원본 사진의 해시와 컷아웃 파일의 파생 관계가 manifest에서 추적돼야 한다.

### HTML 편집성

- 주요 카피를 HTML에서 직접 수정할 수 있어야 한다.
- 이미지와 GIF를 파일 교체만으로 바꿀 수 있어야 한다.
- 색상·폰트·간격을 디자인 토큰에서 일괄 수정할 수 있어야 한다.
- 섹션을 이동·복제·삭제해도 전체 구조가 무너지지 않아야 한다.
- CSS 선언만 보지 않고 실제 브라우저 bounding box, 가로 overflow와 텍스트 clipping을 검증해야 한다.

### GIF

- 한 번의 재생만으로 제품의 한 가지 동작이나 효과를 이해할 수 있어야 한다.
- 갑작스러운 점프, 제품 변형, 로고 변화, 중복 제품이 없어야 한다.
- 무한 루프의 연결이 자연스러워야 한다.
- 파일 크기와 해상도는 실제 HTML 로딩 성능 기준을 통과해야 한다.

## 8. 조사 결과와 남은 조사

### 완료된 조사

- [`Behance 제품 상세페이지 디자인 문법`](research/behance-detail-page-design-grammar.md)
- [`상용 HTML에 흡수할 AI 디자인 스킬 계약`](research/ai-design-skills.md)
- [`HyperFrames 제품 효과 GIF 파이프라인`](research/hyperframes-gif-pipeline.md)

### 남은 조사

- ImageGen 참조 이미지 기반 제품 동일성 유지 방법
- 판매 채널별 HTML, GIF, 외부 리소스 제한

조사는 공식 문서, 공식 저장소, 원본 작품을 우선 출처로 사용한다.

## 9. 현재 확정된 결정

- 기존 `coupang-detail-page-maker`를 기준 스킬로 사용한다.
- 프로젝트 저장소는 비공개 GitHub 저장소 `https://github.com/csm-kr/detail-page-maker-skill`로 관리한다.
- Wayfinder 지도와 결정 티켓은 저장소의 `.scratch/` 아래 로컬 Markdown 이슈로 관리한다.
- 로컬 이슈의 triage 라벨은 `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix` 기본값을 사용한다.
- 최초 입력은 공급처 상품 URL 1개만 받는다.
- 첫 공급처는 도매꾹으로 한정한다.
- 특정 도매꾹 상품번호·판매자 CDN·상세 자산 개수에 고정하지 않고 유효한 임의 도매꾹 상품 URL을 처리한다.
- 도매꾹에서는 원본 사진, 치수·제품 사실, 사실에 연결된 소구 후보와 기획 브리프를 각각 분리해 만든다.
- 공급처 소구와 기획은 제품 사실의 출처로 승격하지 않으며 게시 전 사람의 승인을 요구한다.
- 공급처 원문과 실제 상품 이미지를 SKU 사실 및 제품 동일성의 최우선 기준으로 사용한다.
- 사용자가 촬영한 동일 SKU의 다각도 원본 사진을 제품 사실 SSOT에 추가한다.
- 사용자 촬영 원본에서 배경을 제거한 투명 PNG 제품 컷아웃 SSOT를 가장 먼저 만든다.
- 디자인 합성, ImageGen 제품 장면과 HyperFrames 모션은 승인된 제품 컷아웃 SSOT를 공통 참조로 사용한다.
- ImageGen으로 제품만 보이는 표준 다각도 제품 뷰 시트를 한 뷰씩 만들고 사용자 승인을 받는다.
- ImageGen 제품 뷰 시트는 파생 시각 자산이며 공급처 원문과 사용자 촬영 원본을 덮어쓰지 않는다.
- 최종 산출물은 상용 수준의 수정 가능한 HTML 상세페이지다.
- Behance `상세페이지` 검색 결과를 품질 표본 풀로 사용한다.
- Behance 15개 원본 프로젝트 조사에서 구매 질문 중심 서사, 감성·정보·증거 리듬, 카메라 거리와 복제 방지 규칙을 내부 디자인 계약으로 채택한다.
- AI 디자인은 단일 외부 스킬을 런타임 호출하지 않고 Anthropic·Vercel·DTCG·Playwright·WCAG의 역할별 계약을 내부화한다.
- 시각 생성 AI 모델은 `imagegen`만 사용한다.
- 실제 상품 장면은 제품 참조 이미지를 ImageGen에 전달해 만든다.
- 제품 모션은 HyperFrames로 제작한다.
- HyperFrames가 직접 렌더한 15fps·무음·무한 루프·불투명 GIF를 기본으로 HTML에 삽입한다.
- 30fps MP4는 QA 보존물이며 PNG sequence·FFmpeg 후처리는 용량 초과 시에만 사용한다.
- 한 GIF는 한 가지 제품 작동 또는 효과만 입증한다.

## 10. 아직 결정할 항목

- 프로젝트 규칙 파일을 `AGENTS.md`와 `CLAUDE.md` 중 어디에 둘지
- 기본 판매 채널과 허용되는 HTML/CSS/JavaScript 범위
- 한 페이지에 허용할 GIF 수, 길이, 해상도, 최대 파일 크기
- 현재 내장 편집 패널을 다음 상품에도 기본 제공할지, 채널 납품본에서는 분리할지

## 11. 현재 단계

첫 최종 수정 가능 HTML을 [`prototypes/domeggook-43314131/detail-page/index.html`](prototypes/domeggook-43314131/detail-page/index.html)에 `supplier-reference-v3-commercial`로 구현했다. 공급처 사실 13개와 공개 claim 10개, 동종 제품 실제 후기 기반 문제→해결 기획, ImageGen 소재·동작 프레임, HyperFrames GIF 4개, `content.json`, 승인 범위, 편집 저장 상태와 축소 모션을 연결했다.

Behance 원본 공통 문법과 320·360·390·768·800px Browser Harness QA에서 **97/100, 하드 실패 0개**로 최종 통과했다. 네 GIF의 실제 `.gif` 로드와 서로 다른 시각의 스크린샷 해시 차이를 확인했다. `EXP-001`은 현재 상품에 채택했고, `EXP-003`은 현재 상품에만 유지하며, BIO ORTO의 소재·여백 문법을 변환한 `EXP-004`를 현재 문제→해결 섹션에 채택했다.

사용자 다각도 실사진은 현재 결과의 차단 항목이 아니라 다음 동일성 강화 단계다. 도착하면 현재 HTML 구조와 카피를 유지하면서 실제 사진 기반 제품 시트·누끼·다중 뷰 SSOT로 교체한다.

## 12. 후속 실행 계획

아래 항목은 현재 `43314131` 상세페이지 완성·QA 이후에 실행한다.

### Taste Skill 검증

- [완료] 저장소와 `https://www.tasteskill.dev/`의 실제 계약, MIT 라이선스, v2 파일 구조를 확인했다.
- [완료] `design-taste-frontend`, `gpt-taste`, `redesign-existing-projects`, `image-to-code`, `imagegen-frontend-web`를 Codex 전역 스킬로 설치했다.
- [완료] 현재 페이지의 숫자형 메타 라벨과 눈썹 밀도를 Taste 규약으로 A/B 검수했다. 눈썹은 12→4개, 숫자형 메타 라벨과 em dash는 0개가 됐고, 390px·축소 모션·68개 편집 텍스트가 통과했다.
- [완료] 사용자 피드백에 따라 7.2cm 가로 치수선을 HyperFrames 원본에서 더 위로 올리고 새 GIF·포스터를 게시했다.
- 점수가 실제로 개선된 규칙만 `study-desing-skill.md`와 `commetial-detail-page.md`에 승격한다. 외부 스킬 문구를 무조건 복사하지 않는다.
- 검증 결과와 실패 사례를 누적해 다음 상세페이지 기획 전에 다시 읽는다.

### 정기 디자인 학습 작업

- [완료] Behance `상세페이지` 검색 결과와 Taste Skill 변경 신호를 Browser Harness로 수집하는 로컬 예약 작업을 구현했다.
- [완료] Windows Task Scheduler의 `DetailPageMaker-DesignStudyRefresh`를 매주 월요일 09:30 KST로 등록했다.
- [완료] 예약 실행 경로에서 `LastTaskResult=0`을 확인했다. 다음 실행은 2026-07-27 09:30 KST다.
- 예약 작업은 원본을 자동 복제하거나 상세페이지를 무인 수정하지 않는다. 후보 URL, 게시일, 카테고리, 채택할 디자인 가설을 검토 큐 Markdown으로 갱신한다.
- 사람 또는 에이전트 검토에서 제품 카테고리 적합성, 근거 강도, 편집 가능한 HTML 전환 가능성을 통과한 규칙만 학습 메모리에 승격한다.
- 같은 규칙이 서로 다른 우수 사례 3개 이상에서 재현되고 현재 상품 A/B 시안에서 점수를 높였을 때만 영구 규약으로 채택한다.
- 실행 주기·시간·실행 환경은 예약 작업을 실제로 만들기 직전에 확정한다.

### 도매꾹 인기상품 반복 파이프라인

- 시작 URL은 `https://domeggook.com/main/item/itemPopular.php`로 고정하되, 상품 상세 URL과 상품번호는 매 실행마다 새로 확인한다.
- 인기상품 후보를 한 번에 대량 제작하지 않고 한 상품씩 다음 전 과정을 완료한다.
  1. 후보 상품과 공급처 상세 URL 확정
  2. 공급처 원본 사진·치수·재질·구성·소구 문구·기획 단서 추출
  3. 동종 제품 최소 3개와 실제 공개 후기 조사
  4. 사용자 불편을 `OWN_REVIEW | MARKET_PAIN | SYNTHETIC_PAIN`으로 구분
  5. `상황 → 불편 → 제품 구조가 해결하는 방식 → 시각 근거 → 사용 주의` 서사 기획
  6. 실제 사진 기반 제품 누끼와 다각도 제품 SSOT 제작
  7. 필요한 장면만 ImageGen 참조 이미지 방식으로 생성
  8. 치수·구조·사용 동작을 HyperFrames GIF로 제작
  9. 주요 카피·이미지·색상·섹션을 수정할 수 있는 HTML 상세페이지 제작
  10. Behance 상용 루브릭, 제품 동일성, 주장-근거, 반응형, GIF 실재생 QA
  11. 97점 이상·하드 실패 0개일 때만 완료 처리
- 동종 후기에서 확인되지 않은 생활 불편은 `SYNTHETIC_PAIN`으로 명시하고 실제 구매자 발화처럼 꾸미지 않는다.
- 경쟁 제품의 불편이 현재 제품 구조로 해결되지 않으면 해결 소구로 사용하지 않고 사용 주의 또는 기획 제외 사유로 남긴다.

## 13. 도매꾹 인기상품 첫 실행: 66475839

- [완료] 인기상품 목록의 첫 실제 상품인 아쿠아슈즈 `66475839`를 선택했다.
- [완료] 대표 이미지, 800×12,402px 상세 원본, 공개 후기 0건을 `dmk-extractor`로 추출하고 검증했다.
- [완료] 그린·그레이, S~XXXL 발길이 표, 약 1.5cm 바닥 두께, L 기준 약 197g, 중국산, 뒤꿈치 밴드와 바닥 패턴을 사실로 정규화했다.
- [완료] 통기·빠른 건조·미끄럼 방지·충격 흡수·720도 유연성은 시험 전 공급처 주장으로 격리했다.
- [완료] `족저근막염 예방` 문구는 질환 예방 주장으로 판정하고 판매 기획에서 완전히 제외했다.
- [완료] 동종 아쿠아슈즈 3개의 공개 후기에서 사이즈, 뒤꿈치·봉제, 젖은 바닥 불안, 모래 유입, 배수 체감, 손상, 색상 문제를 교차 확인했다.
- [완료] 공급처 원본을 참조한 ImageGen 제품 컷아웃과 착화 장면을 만들고, 보이지 않는 밑창을 추정한 시안은 증거 사용에서 탈락시켰다.
- [완료] HyperFrames로 발길이 선택, 바닥 기준 약 1.5cm, 착화 맥락 GIF 3개를 만들었다. 두께 모션의 위 가로 기준선은 제품과 닿지 않도록 22px 올렸다.
- [완료] 카피 100개, CSS 토큰, 저장·JSON 내보내기·모션 토글을 제공하는 편집형 HTML을 제작했다.
- [완료] 320·360·390·768·800px Browser Harness QA, 실제 GIF 프레임 해시 검증, Behance 상용 루브릭 **97/100·하드 실패 0개**로 통과했다.

## 14. 상업 완성도 강화와 다음 상품: 23824901

### 아쿠아슈즈 후속 교정

- [완료] 10개 섹션을 16개로 확장하고 수영장·워터파크·해변·계곡의 ImageGen 사용 맥락 장면 5개를 각각 별도 생성했다.
- [완료] 이미지 위 문구는 HTML로 분리하고 편집 항목을 100개에서 117개로 늘렸다.
- [완료] 별도 `studio.html`에서 문구·크기·정렬·색상·이미지·초점·오버레이·토큰을 수정하고 JSON과 이미지 내장 단일 HTML을 내보내도록 했다.
- [완료] 첫 소구를 발길이 안내가 아니라 “수영장부터 계곡까지 쓰는 물놀이 한 켤레”로 교정하고, 발길이는 선택을 돕는 후반 정보로 내렸다.
- [완료] 발길이 HyperFrames의 바 끝·점 중심·활성 카드가 같은 좌표와 도착 시점을 사용하도록 수정했다. 2.0초 M, 2.3초 L, 4.5초 XXXL에서 일치하며 check 오류·경고 0개다.

### 새 도매꾹 상품 전체 파이프라인

대상: `https://domeggook.com/23824901?from=popular100`

1. 새 경로에 공급처 대표·상세 원본·치수·재질·구성·주장·기획 단서를 추출하고 검증한다.
2. 공급처 사실과 보이는 제품 구조를 잠근 뒤 핵심 소구 후보를 “승인 사실 × 고객이 원하는 변화 × 시각 증명 가능성”으로 순위화한다.
3. 이전에 조사하지 않은 Behance 우수 상세페이지에서 타이포 크기, 정렬, 사진 위 카피, 감성→정보→증거 리듬을 추가 관찰한다.
4. 실제 상품 사진을 누끼 SSOT로 만들고, ImageGen에는 동일 SKU 참조를 넣어 문자가 없는 장면·장식·FX 원본을 약 5종 만든다.
5. 불편 제기는 좌우 SVG 말풍선으로 만들되 공개 후기와 합성 불편을 구분하고 가짜 구매자 신원을 만들지 않는다.
6. 문제·준비·작동·치수·관리의 서로 다른 역할을 가진 HyperFrames GIF 5개를 만든다.
7. 실제 제품, 수정 가능한 HTML·SVG 정보, ImageGen 장식의 세 레이어를 분리한 상세페이지와 Studio를 구현한다.
8. 320·360·390·768·800px, 실제 GIF 재생, 단일 HTML 내보내기, 한국어 카피, 글자 크기, 선·점·말풍선 정렬을 검수한다.
9. `commercial-tight v2`에서 95점 이상, 배포 목표 97점 이상, 하드 실패 0개일 때만 완료한다.

### 사용자 피드백 회귀 체크

- 핵심 소구가 치수·주의 문구 같은 보조 정보로 시작하지 않는가
- 구매자용 본문·캡션이 14px 아래로 내려가지 않는가
- 밝은 사진 위 카피에 로컬 오버레이와 안전 여백이 있는가
- 치수선이 제품에 닿지 않고 바 끝·점 중심 오차가 2px 이내인가
- 활성 라벨이 포인터 도착 전에 바뀌지 않는가
- 말풍선 꼬리가 실제 대상에서 8px 이상 벗어나지 않는가
- ImageGen 장식이 제품 사실이나 시험 근거처럼 사용되지 않는가
- 다섯 GIF가 각각 다른 구매 질문을 답하고 실제로 재생되는가
- 제작 메모형 말투와 공급처 관리 문구가 구매자 화면에 남지 않는가
## 15. 도매꾹 23824901 아이스 쿨패치 완료 기록

- [완료] 도매꾹 URL 하나에서 대표 이미지, 상세 원본, 치수, 재질, 구성, 사용법, 주의사항, 공개 후기 7건을 추출하고 검증했다.
- [완료] 첫 소구를 치수·냉장 준비가 아니라 **“더운 순간, 손에 들지 않고 붙이는 제품 형태”**로 확정했다.
- [완료] 유사 제품 공개 검색 결과에서 `냉팩을 계속 들고 있는 불편`, `땀·움직임에서 들뜸 걱정`, `냉각 강도 개인차`, `피부 자극 우려`, `의약품 혼동`을 MARKET_PAIN으로 분리했다.
- [완료] 현재 SKU가 해결한다고 말할 수 있는 범위를 제품 형태·사용 순서로 제한했다. 접착력·지속 시간·저자극은 해결 주장으로 쓰지 않았다.
- [완료] 제품 앞면과 필름 반제거 상태를 ImageGen 참조 생성 → 단색 마젠타 → 결정론적 크로마키로 RGBA 누끼 SSOT화했다.
- [완료] ImageGen 소재 5종을 히어로 분위기, 야외 사용 맥락, 피부 준비, 냉장 준비, 하이드로겔 매크로에 각각 배치했다.
- [완료] HyperFrames 모션 스튜디오에서 문제·준비·필름 제거·치수·주의의 서로 다른 5개 GIF를 만들었다.
- [완료] 치수 GIF는 선 끝점과 점 중심 오차 0px, 제품과 가로 24px·세로 60px 간격, 선 도착 뒤 라벨 전환으로 검증했다.
- [완료] 13개 섹션, 편집 카피 116개, 교체 이미지/GIF 13개, GIF 5개인 `index.html`을 만들었다.
- [완료] 별도 `studio.html`에서 카피·글자 크기·정렬·색상·이미지·GIF·초점·테마 토큰을 수정하고 JSON·단일 HTML로 내보내게 했다.
- [완료] 단일 HTML 39,857,084바이트에 이미지 13개와 GIF 5개를 내장하고 재오픈해 깨진 이미지 0개를 확인했다.
- [완료] Browser Harness 320·360·390·768·800px에서 overflow 0, 최소 글자 14px, 깨진 이미지 0, alt 누락 0을 확인했다.
- [완료] commercial-tight v2 **98/100, 하드 실패 0개**로 게시 승인했다.

### 게시 차단 유지

- `-5°C`, `-10.1°C`, `최대 8시간`
- 강한 접착, 들뜸 없음
- 무향, 저자극, 무자극
- 기존 제품 대비 33%·40%
- 해열·치료 효능
- 원본 번호·범위 확인 전 KC·KATRI 배지

### 다음 반복

도매꾹 인기상품 반복 루프는 계획에 유지하되 자동으로 새 상품을 시작하지 않는다. 다음 공급처 URL 또는 사용자가 촬영한 실제 제품 사진이 들어오면 동일한 SSOT·5역할 모션·Studio·commercial-tight v2 게이트를 적용한다.

## 17. 노바페이스 발편한 기능성깔창 60851997

### 현재 완료 범위

- 실제품 다각도 촬영본과 사용자 승격 `Flux2-Klein_00355_.png`를 상면·하면·에어홀·에어셀·블루쿠션의 정체성 SSOT로 잠갔다.
- 실제품 촬영본과 거친 누끼는 공개 화면에서 제외하고, 다중 참조 ImageGen 상용 소재로 교체했다.
- 상용 소재는 하부 한 켤레, 상면 한 켤레, 통풍, 쿠션, 뒤꿈치, 굽힘, 신발 삽입의 일곱 역할로 분리했다.
- 굽힘 장면의 블루쿠션 위치 오류를 발견한 뒤, `하면 뒤꿈치 중앙`, `전체 길이의 약 18–20%`, `뒤꿈치 폭의 약 60%`, `흰 셀 테두리 유지` 조건으로 재생성했다.
- 같은 구조 감사에서 신발 삽입 장면의 상면 블루쿠션 오류도 찾아 회색 에어메시·회색 힐 패치로 재생성했다.
- HyperFrames GIF 8개를 상면/하면, 셀, 통풍, 굽힘, 사이즈, 신발 삽입, 한 켤레, 소재 비교로 제작했다.
- 구매자 화면의 제작 메타 문구를 모두 제거하고, 질문 3개와 사용자 관점 공감 카피를 추가했다.
- 브랜드명 `노바페이스`, 제품명 `발편한 기능성깔창`을 히어로·제품 정보·마감 구간에 고정했다.

### 이후 Studio 확장 TODO — 현재 프로토타입 완료 뒤 별도 진행

- `https://github.com/heygen-com/hyperframes`의 프레임·타임라인·선택 상태·속성 패널 기능을 별도 조사한다.
- 상세페이지 Studio에 전역·섹션별 글씨체, 글자 크기, 행간, 자간, 글씨색, 배경색, 정렬, 안전영역을 수정하는 속성 패널을 설계한다.
- 색·타이포 토큰의 변경 범위와 상속 관계를 보여주고 되돌리기·버전 비교·JSON preset을 제공한다.
- GIF/HyperFrames 장면을 섹션 안에서 재생·정지·대표 프레임 선택·교체할 수 있게 한다.
- 이 TODO는 현재 노바페이스 광고 QA와 배포를 끝낸 뒤 별도 결정 티켓으로 착수한다.

### 2026-07-25 상용 완성도 재개

- 신규 Behance 상용 상세 10건을 상단뿐 아니라 내부 25%·50%·75%와 GIF 모듈까지 분석한다.
- 10건 중 8건, 총 60개 GIF에서 확인한 역할 분리 원칙을 적용한다.
- 기존 23개 섹션의 `정지 설명 → 같은 내용의 정사각 GIF` 반복을 줄여 18개 섹션으로 재구성한다.
- GIF 8개는 유지하되 제품 뒤집기, 쿠션 전후, 에어홀 흐름, 굽힘, 사이즈 선택, 삽입, 구성, 소재 비교로 질문을 분리한다.
- `Flux2-Klein_00355_.png`를 사용자 승격 SSOT로 기록한다.
- 통풍 효과는 앞꿈치·중족부 에어홀에서만 시작하고 블루쿠션에는 바람·광선·통풍 입자를 금지한다.
- 설치 스킬의 `skills/detail-page-maker-skill/SKILL.md`는 실행 흐름과 하드 게이트만 남기고 제품 동일성, HyperFrames GIF, 한글 카피, Behance 분석, 사용자 피드백을 스킬 내부 `references/`로 분리한다.

## 16. 기능성 인솔 44358530 확장 미디어 프로토타입

- [완료] 도매꾹 `44358530`의 대표·상세·시연 GIF를 추출하고 제품명, EVA·라이크라, 국산, 재단→가열→5분 착화, S–XL, 적응·보관·중단 기준을 FACT-001~015로 잠갔다.
- [완료] 평발·족저근막염·통증 치료·교정, 충격 흡수·압력 분산, 전 신발 호환, 즉시 편안함, 1–2년 보장은 게시 차단했다.
- [완료] 공급처 원본을 참조한 제품 다각도·한 쌍·측면 누끼 SSOT 3개와, 히어로·문제·소재·재단·가열·착화·일상·관리 ImageGen 장면 8개를 만들었다.
- [완료] HyperFrames로 형태 완성·재단·가열·5분 착화·측면 변화·사이즈·적응의 일곱 GIF를 만들고 800×800, 78프레임, 5.2초로 검증했다.
- [완료] 30fps·1,092프레임·36.4초 QA 마스터 MP4를 별도로 보존했다.
- [완료] 20개 섹션, 편집 텍스트 88개, 안정된 asset ID를 가진 이미지 21개, GIF 10개를 포함한 수정 가능한 `index.html`과 `studio.html`을 만들었다.
- [완료] Studio에서 텍스트 수정, 이미지 경로·대체 텍스트 교체, 강조색, 로컬 저장, GIF 재시작, HTML 내보내기를 구현했다.
- [완료] 320·360·390·768·800px에서 scrollWidth 일치, overflow 0, 깨진 이미지 0을 확인했다.
- [완료] HyperFrames check 오류·경고 0, 대비 42/42, HTML validate 0, commercial-tight v3 **98/100·하드 실패 0개**로 승인했다.

### 이 반복에서 추가한 게이트

- 기능성 상품의 첫 소구는 질환·효능이 아니라 사용자가 실제로 바꾸는 과정 또는 구조여야 한다.
- 긴 상세페이지의 미디어 양은 장수로만 정하지 않고 `첫 인지 / 문제 / 원리 / 사용 / 수치 / 실제 시연 / 적응·주의` 질문의 커버리지로 정한다.
- 생성 장면, 제품 SSOT, 실제 공급처 시연은 역할을 섞지 않는다. 생성 장면은 맥락, SSOT는 동일성, 실제 시연은 움직임 사실을 맡는다.
- 공급처 이미지에 근거보다 강한 문구가 구워져 있으면 필요한 제품 시각 부분만 크롭하거나 사용하지 않는다.
- 모바일 QA는 문서 scrollWidth만 보지 않는다. `overflow:hidden`으로 가려진 자식 요소도 각 요소의 실제 bounding box로 검사한다.

### 2026-07-25 노바페이스 commercial-final v8 완료

- 18개 섹션, 편집 텍스트 95개, 정적·동적 이미지 13개, 실제 GIF 8개로 최종 HTML을 마감했다.
- 후기처럼 읽히는 소구 메시지를 `오래 서는 날의 포근한 지지 / 에어홀 통기 / 쉬운 사이즈 선택` 세 역할로 추가했다. 실제 후기처럼 보이는 별점·작성자·구매 인증은 만들지 않았다.
- 말풍선 본체와 SVG 꼬리를 같은 불투명 흰색으로 통일해 접합부 이음선을 제거했다.
- 에어홀·블루쿠션·유연함 장면마다 `ZOOM SPORTS` 인쇄를 확인하고, 생성 이미지에 누락된 장면은 승인된 블루쿠션 좌표의 HTML·HyperFrames 오버레이로 보정했다.
- 통풍 이펙트는 앞꿈치·중족부 에어홀에서만 시작하고 구멍이 없는 블루쿠션에는 배치하지 않았다.
- HyperFrames master v8은 800×800, 30fps, 38.4초, 1,152프레임이며 check 결과 lint·runtime·layout·motion 0, contrast 35/35다.
- 게시 GIF는 800×800, 12fps, 각 4.83초·58프레임으로 재생성하고 각 파일·master의 SHA-256을 manifest에 기록했다.
- Browser Harness에서 320·360·390·768·800px의 overflow·경계 밖 요소·잘린 텍스트·깨진 이미지가 모두 0임을 확인했다.
- HTML validator 0, duplicate ID 0, duplicate image source 0, 금지 제작 메타 0건을 확인했다.
- Studio 편집 모드를 실제로 켜고 95개 contenteditable 활성화, 텍스트 변경·원문 복원, 편집 모드 해제를 확인했다.
- 최종 타이트 루브릭은 **98/100, 하드 실패 0개**다. 근거는 `prototypes/domeggook-60851997/qa/commercial-final-v8.md`에 기록한다.

### 2026-07-25 노바페이스 image-led commercial-final v9 완료

- [완료] ImageGen 신규 소재 12장을 제작했다: 제품 인쇄 보정 3장, 윗면·아랫면 비교 2장, 군화·작업화·운동화·일상화 4장, 문제·통풍·마감 배경 3장.
- [완료] 네 사용 상황을 독립 사진 카드로 교체하고 2열 데스크톱·1열 모바일 레이아웃에 통합했다.
- [완료] `ZOOM SPORTS` 흰 HTML·HyperFrames 오버레이를 제거하고 제품 이미지 안에 인쇄가 포함된 소스로 바꿨다.
- [완료] 공개 HTML·alt·GIF 카피의 `상면 / 하면`을 `윗면 / 아랫면`으로 교체했다.
- [완료] 소재 비교 GIF의 분할선·커서를 `+224px → -224px → 0px`로 움직여 양 끝 상태와 최종 50:50 상태를 명확히 했다.
- [완료] 정적 삽입 섹션과 삽입 GIF 중복을 하나로 합쳐 18개 섹션을 17개로 줄였다.
- [완료] FAQ 4개를 모두 펼친 상태로 제공했다.
- [완료] HyperFrames master v9을 800×800, 15fps, 38.4초로 렌더링했다. strict check는 lint·runtime·layout·motion 0, contrast 35/35다.
- [완료] GIF 8개를 800×800, 12fps, 각 4.83초·58프레임으로 게시하고 SHA-256 manifest와 8×3 접촉판을 갱신했다.
- [완료] Browser Harness 320·360·390·768·800px에서 overflow·경계 밖 요소·잘린 텍스트·깨진 이미지 0을 확인했다.
- [완료] HTML validator 0, GIF 8개 currentSrc, FAQ 4/4 open, 제품 인쇄 텍스트 오버레이 0을 확인했다.
- [완료] Studio 수정 모드에서 편집 텍스트 90개와 이미지 17개가 활성화되는 것을 확인했다.
- [완료] 이번 피드백을 당시 기준 스킬 문서와 네 개의 분리 규약, `commetial-detail-page.md`, `study-desing-skill.md`에 반영했다. 현재 설치 진입점은 `skills/detail-page-maker-skill/SKILL.md`다.

### 2026-07-25 노바페이스 commercial-final v10 완료

- [완료] 윗면 한 쌍을 동일 길이·외곽·표면 패턴과 진짜 좌우 거울 대칭 아치로 재생성했다.
- [완료] `ZOOM SPORTS`를 정확한 철자·두 줄 구성·블루쿠션 장축·제품 긴 방향으로 검수하고 `STORTS` 회귀를 차단했다.
- [완료] 통풍 효과를 앞꿈치·중족부 에어홀에만 두고 블루쿠션에서는 제거했다.
- [완료] HyperFrames 9번째 `착화 후 보행` 장면을 추가하고 43.2초 master와 역할이 다른 GIF 9개를 게시했다.
- [완료] GIF 상태 라벨을 18px·700 이상으로 키우고 굽힘 곡선을 제품 위쪽으로 이동했으며 260 선택 포인터의 도착·눌림·전환을 구현했다.
- [완료] 군화·작업화·운동화·일상화 4장을 착화 행동으로 구성하고 각 카드에 실제 제품 인셋을 추가했다.
- [완료] 피날레에 네 신발과 제품 한 쌍을 함께 배치하고, 별도 착화 후 보행 이펙트로 상업 마감을 보강했다.
- [완료] 18개 섹션, 이미지 23개, GIF 9개, 편집 텍스트 96개, 교체 자산 23개로 갱신했다.
- [완료] HyperFrames strict 오류·경고 0, contrast 32/32, HTML validator 0, 깨진 이미지 0, 320~800px 가로 overflow 0을 확인했다.
- [완료] 상용 타이트 루브릭 **98/100·하드 실패 0개**로 승인했다.

### 후속: 레이어형 상세페이지 Studio

- [TODO] 말풍선 본체·SVG 꼬리·텍스트·GIF·이미지·강조 박스를 안정된 layer ID로 분리한다.
- [TODO] `x/y/width/height/z-index`, 글자 정렬·행간, 색·곡률, 꼬리 방향·끝점, 이미지 crop·`object-position`, GIF poster·재시작을 inspector에서 수정하게 한다.
- [TODO] 8px 그리드, edge/center 스냅, 안전영역과 320·390·800px별 오버라이드로 얼라인을 보존한다.
- [TODO] 저장은 section ID + layer ID 기반 JSON preset으로 하고 undo/redo·버전 비교·독립 HTML 내보내기를 연결한다.
- [TODO] 현재 노바페이스 상세페이지 완료와 분리한 `.scratch` 결정 티켓에서 기술 검토와 프로토타입을 진행한다.

### 2026-07-26 노바페이스 commercial-final v11 교정 완료

- [완료] 아랫면 흰 PU 본체의 잘못된 `STORTS` 각인을 모든 공개 소재에서 정확한 `SPORTS`로 교체했다.
- [완료] 굽힘 소재의 `ZZOM/ZZOOM` 회귀를 제거하고 블루쿠션 인쇄를 정확한 `ZOOM SPORTS`로 다시 만들었다.
- [완료] 뒤꿈치 확대의 인쇄 기준선을 블루쿠션과 깔창 장축에 맞췄다.
- [완료] 굽힘 안내선은 제품의 실제 휘어짐을 따라 아래로 볼록한 U자 곡선으로 바꿨다.
- [완료] 255mm→260 선택 화살표는 두 텍스트 사이의 실제 시각 간격을 좌우 동일하게 맞췄고, 260 포인터를 카드 중앙에 정렬했다.
- [완료] 사이즈 GIF의 하단 정보바를 첫 프레임부터 유지해 빈 하단을 줄였다.
- [완료] 군화·작업화·운동화·일상화 맥락 카드에서 작은 제품 인셋을 제거하고 착화 장면과 카피에 집중시켰다.
- [완료] HyperFrames 9개 장면의 폰트 스택·굵기·제목 크기를 HTML과 통일하고 의미 단위 줄 간격을 확보했다.
- [완료] master v11 800×800·15fps·43.2초와 게시 GIF 9개 800×800·12fps·58프레임을 다시 렌더링했다.
- [완료] HyperFrames strict 오류·경고 0, contrast 34/34, HTML validator 0, JSON 4개 파싱 0, 320·360·390·767·800px overflow·경계 밖·잘린 텍스트 0을 확인했다.
- [완료] 최종 결과는 **98/100·하드 실패 0개**로 승인한다.

## 17. 2026-07-26 Detail Page Studio v2 첫 실행형 구조

- [완료] 설치 단위를 `skills/detail-page-maker-skill/` 하나로 정리하고 루트의 중복 스킬 진입점을 제거했다.
- [완료] `npx skills add csm-kr/detail-page-maker-skill --full-depth`로 발견되는 단일 스킬 안에 `SKILL.md`, `agents/`, `scripts/`, `references/`, `assets/`를 모두 포함했다.
- [완료] Node.js 22+와 HyperFrames 설치 상태를 확인하는 `doctor`, 공급처 URL로 프로젝트를 만드는 `new`, 로컬 Studio를 여는 `start` 명령을 구현했다.
- [완료] 기본 프로젝트 위치를 `Documents/DetailPageStudio/projects/<상품명>-<상품번호>/`로 고정했다.
- [완료] 에셋 검수 → GIF 레이어 편집 → 조립 잠금 → HTML 편집 → 최종 QA의 다섯 작업면을 하나의 `studio.html`로 통합했다.
- [완료] 제품 동일성 하드 실패 차단, 사용자 승인, 에셋 버전·해시 잠금, 의존성 기반 개정판, 97점 게시 게이트를 서버 불변식으로 구현했다.
- [완료] 원본·후보 나란히 보기, 투명도 겹치기, 차이 강조, 동기 스크롤 확대, GIF 동시 재시작과 QA MP4 동일 시점 탐색을 구현했다.
- [완료] HyperFrames 레이어의 텍스트·x/y·크기·투명도·폰트·색을 조정해 새 후보 렌더 작업으로 보내는 속성 패널을 구현했다.
- [완료] HTML 안정 layer ID, 320·390·800px 오버라이드, 자동 저장, 세션 undo/redo, 이름 있는 체크포인트를 구현했다.
- [완료] QA 전 검토용 초안, QA 97점·하드 실패 0건·사용자 승인 뒤 게시용 단일 HTML, 전체 Studio 프로젝트 묶음을 분리했다.
- [완료] 도메인·런타임 회귀 테스트 7개와 Browser Harness 화면 검수를 통과했다.
- [완료] 기존 상용 규약·Behance 분석·한글 카피·제품 동일성·HyperFrames·사용자 피드백·도매꾹 계약을 설치 스킬 내부 `references/`로 복제해 스킬 폴더만으로 학습 메모를 재사용하게 했다.
- [보존] 기존 `prototypes/`, `videos/`, `research/`, `tests/` 산출물은 삭제·이동하지 않고 설치 스킬 탐지에서 제외했다. 물리적 `lab/` 이동은 기존 상대 경로를 깨뜨릴 수 있으므로 별도 마이그레이션으로 남긴다.
